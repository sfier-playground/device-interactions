name: CI
on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [develop, master]
jobs:
  quality-verification:
    steps:
      - uses: ./github/workflows/jobs/go_quality_verification.yml@dev
        secrets: inherit
